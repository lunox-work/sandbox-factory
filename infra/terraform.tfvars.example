# Optional: every input has a working default in variables.tf. To override,
# copy this to terraform.tfvars (gitignored) and uncomment what you need.
#
#   cp terraform.tfvars.example terraform.tfvars

# Alarm recipient; defaults to devops@lunox.work. AWS delivers nothing until
# the link in its confirmation mail is clicked.
# alarm_email = "you@example.com"

# One task is a single point of failure; two removes it.
# api_desired_count = 2
