{
  "name": "@sandbox-factory/db",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rm -rf dist tsconfig.tsbuildinfo && tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.test.json --noEmit",
    "test": "rm -rf dist-test && tsc -p tsconfig.test.json && node --experimental-test-coverage --test-coverage-lines=90 --test-coverage-branches=90 --test-coverage-functions=90 --test-coverage-include=\"dist-test/src/**\" --test-coverage-exclude=\"dist-test/test/**\" --test dist-test/test/*.test.js > dist-test/coverage.txt; status=$?; cat dist-test/coverage.txt; [ $status -eq 0 ] && assert-all-covered dist-test/src dist-test/coverage.txt index.js migrate-cli.js",
    "test:bounty-concurrency": "rm -rf dist-test && tsc -p tsconfig.test.json && node --test dist-test/test/bounty-concurrency.integration.js",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "node dist/migrate-cli.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1133.0",
    "@aws-sdk/s3-request-presigner": "^3.1138.0",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.5",
    "sandbox-factory": "*"
  },
  "devDependencies": {
    "@sandbox-factory/coverage": "*",
    "@sandbox-factory/tsconfig": "*",
    "@types/node": "^26.6.2",
    "drizzle-kit": "^0.31.11",
    "typescript": "^7.0.2"
  }
}
