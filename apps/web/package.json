{
  "name": "@sandbox-factory/web",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && vite build",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"No web tests yet — add Vitest when the first component earns one.\"",
    "dev": "vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "@sandbox-factory/client": "*",
    "@sandbox-factory/shared": "*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "sandbox-factory": "*"
  },
  "devDependencies": {
    "@sandbox-factory/tsconfig": "*",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^7.0.2",
    "vite": "^6.0.5"
  }
}
